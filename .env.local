# Local Development Environment Configuration
# Copy this file to .env.local and adjust values as needed

# Node environment
NODE_ENV=development

# Server port (default: 5000)
# Change this if port 5000 is already in use on your machine
PORT=5000

# Database URL (optional for local development)
# Currently using in-memory storage, so this is not required
# If you want persistent data, set up PostgreSQL and use:
# DATABASE_URL=postgresql://user:password@localhost:5432/paws_paychecks
# For now, this is a dummy value
DATABASE_URL=postgresql://localhost:5432/paws_paychecks_dev
